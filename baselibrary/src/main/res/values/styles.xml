<resources xmlns:tools="http://schemas.android.com/tools">

    <declare-styleable name="title">
        <attr name="name" format="string"/>
    </declare-styleable>

    <!-- Base applicationz theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorDb3</item>
        <item name="colorPrimaryDark">@color/colorWhite</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">false</item>

        <!--无标题-->
        <item name="android:windowActionBar">false</item>
    </style>

    <style name="Tabtext">
        <item name="android:textColor">@color/selector_tab_text</item>
        <item name="android:textSize">@dimen/sp_18</item>
    </style>

    <style name="TabtextDb">
        <item name="android:textColor">@color/selector_tab_text_db</item>
        <item name="android:textSize">@dimen/sp_18</item>
    </style>

    <style name="TabtextBlack">
        <item name="android:textColor">@color/selector_tab_text_black</item>
        <item name="android:textSize">@dimen/sp_21</item>
    </style>


    <style name="TransparentDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowBackground">@color/lsq_color_transparent</item>
    </style>

    <style name="dialog_animation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_out</item>
    </style>




    <style name="dialog" parent="@android:style/Theme.Dialog">
        <!-- 是否有边框 -->
        <item name="android:windowFrame">@null</item>
        <!--是否在悬浮Activity之上  -->
        <item name="android:windowIsFloating">true</item>
        <!--标题  -->
        <item name="android:windowNoTitle">true</item>
        <!--阴影  -->
        <item name="android:windowIsTranslucent">false</item>
        <!--透明背景-->
        <item name="android:background">@android:color/transparent</item>
        <!--窗口背景透明-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--弹窗背景是否变暗-->
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="dialog2">
        <!--弹窗背景是否变暗-->
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
    </style>

</resources>
